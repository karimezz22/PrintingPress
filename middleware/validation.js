// Middleware for handling validation Request

function validateRequest(req, res, next) {
    // Implement request validation logic here
    // Validate request parameters, body, headers, etc.
    // If the request is valid, proceed to the next middleware or route handler
    // If the request is invalid, send a validation error response
  }
  
  module.exports = {
    validateRequest
  };